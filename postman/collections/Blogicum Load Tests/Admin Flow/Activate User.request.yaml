$kind: http-request
url: "{{baseUrl}}/admin/user/activate/{{temp_username}}"
method: POST
headers:
  Authorization: Bearer {{adminToken}}
scripts:
  - type: afterResponse
    code: pm.test("User activated", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 2000
